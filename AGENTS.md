<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

<!-- System Environment -->
# Environment: Windows 11
# Shell: Git Bash (GNU bash 5.1.16)
# Workspace: c:\Users\nederbird\projects\home-stats-app
# Available tools: git, npm, python, node, code

<!-- Command Execution Note -->
To use Git Bash commands (ls, grep, etc.) in the execute_command tool, prefix with `bash -c "command"`.
Example: `bash -c "ls -la"`
The execute_command tool runs in PowerShell by default, so Linux-style commands need bash invocation.
